/*
问题：析构函数是什么？合成析构函数完成什么工作？什么时候会生成合成析构函数？

析构函数：类的特殊成员函数，名字是 ~类名()，没有返回值、没有参数；对象销毁时自动调用。
作用：释放对象在生命周期内获取的资源（堆内存、文件句柄等）。

析构函数执行顺序：先执行函数体，然后按逆序销毁对象的所有非 static 成员。

合成析构函数（编译器自动生成）：函数体为空。只负责销毁对象的成员：依次调用每个成员自身的析构函数。

合成析构函数不会 delete 裸指针！裸指针成员指向的堆内存不会自动释放。

生成时机：当类没有显式定义自己的析构函数，编译器就会生成合成析构函数。
*/
