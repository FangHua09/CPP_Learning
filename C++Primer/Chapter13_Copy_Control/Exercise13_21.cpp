/*
题目：TextQuery 和 QueryResult 需要自定义拷贝控制成员吗？为什么？✅ 不需要自定义拷贝构造、拷贝赋值、析构。理由：
两个类没有裸指针，所有动态资源都交给shared_ptr智能指针管理；
合成拷贝控制成员，会自动拷贝 shared_ptr，正确增减引用计数；
业务场景：允许多个 TextQuery/QueryResult 对象共享同一份文本数据和行号集合，不需要深拷贝；
shared_ptr 会自动管理内存释放，合成析构足够完成资源释放。
补充：没有资源泄漏风险，不需要自己写拷贝控制成员。

*/
