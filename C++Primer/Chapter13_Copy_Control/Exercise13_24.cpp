/*
题目：如果本节的 HasPtr 版本未定义析构函数，会发生什么？如果未定义拷贝构造函数，会发生什么？
⚠️ 这里 HasPtr 就是你上面这份【值语义 HasPtr】，成员 string *ps; int i;
如果没有自定义析构函数
编译器生成合成析构函数。合成析构只会销毁成员变量ps、i本身。
ps只是一个裸指针变量，合成析构不会执行 delete ps。
堆上new string分配的内存永远不会被释放，内存泄漏。
如果没有自定义拷贝构造函数
编译器生成合成拷贝构造函数，做逐成员浅拷贝：直接复制指针ps的值。

HasPtr a("hi", 1);
HasPtr b = a; // 合成拷贝：b.ps = a.ps;
此时a.ps和b.ps指向同一个堆 string。
当对象销毁时：先析构其中一个对象，执行delete ps释放这块内存；剩下另一个对象的ps变成野指针。等到第二个对象析构，再次delete ps，对野指针 delete，未定义行为，程序崩溃。
*/
