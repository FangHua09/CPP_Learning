/*
类指针版本（引用计数版 HasPtr）会从 swap 函数受益吗？如果受益，得到什么好处；如果不是，为什么？
✅ 会受益。
类指针版本（引用计数 HasPtr，成员string *ps; int *use; int i;）：自定义 swap 只交换ps、use、i这几个指针 / 内置变量的值。
不需要拷贝string，不需要修改引用计数！
不用增减*use，不用分配、释放堆内存。
对比直接赋值（拷贝赋值）：拷贝赋值要修改引用计数，有可能触发 delete 释放资源，开销更大。
swap 只是交换两个对象内部的指针成员，常量时间 O (1)，开销远小于拷贝。
*/
