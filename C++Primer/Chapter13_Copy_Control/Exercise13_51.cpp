/*
练习 13.51：unique_ptr不能拷贝，但函数按值返回unique_ptr合法

知识点：返回值优化 (RVO) + unique_ptr 有移动构造
unique_ptr删除了拷贝构造，所以不能复制；但是它提供了移动构造函数。
函数以值返回局部unique_ptr对象时，这个局部对象是即将销毁的右值。
返回时，不会拷贝，而是调用unique_ptr的移动构造，把所有权转移出去；编译器还会触发返回值优化 RVO，直接在调用方内存构造对象，连移动都省略。
一句话：不能拷贝，但可以移动；函数返回局部对象是右值，执行移动，所以合法。
*/
