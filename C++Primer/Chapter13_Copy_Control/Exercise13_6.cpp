/*
题目描述：拷贝赋值运算符是什么？什么时候使用它？合成拷贝赋值运算符完成什么工作？什么时候会生成合成拷贝赋值运算符？
知识点：拷贝赋值运算符、合成拷贝赋值运算符、类的拷贝控制成员
解答
拷贝赋值运算符：是一个重载的=运算符，是类的成员函数，形式为类名& operator=(const 类名&);。接收一个本类型的 const 引用，返回当前对象的引用。
使用时机：当对类对象执行赋值操作时调用，例如obj1 = obj2;。注意区分：拷贝构造是创建新对象时；拷贝赋值是两个已经存在的对象之间赋值。
合成拷贝赋值运算符：编译器自动生成，将右侧对象的每个非 static 成员，逐个赋值给左侧对象对应的成员。内置类型直接赋值；类类型成员调用自身的拷贝赋值运算符。
生成时机：当类没有显式定义自己的拷贝赋值运算符时，编译器会合成拷贝赋值运算符。
补充：如果类定义了移动构造 / 移动赋值，部分新版本标准下，合成拷贝成员不会生成。
*/
