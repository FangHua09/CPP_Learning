/*
题目描述：下面的哪个声明是错误的？为什么？
(a) int ff(int a, int b = 0, int c = 0);
(b) char *init(int ht = 24, int wd, char bckgrnd);

(a) ✅合法。默认实参从形参列表最右侧开始，b、c给了默认值，a无默认，顺序正确。
(b) ❌错误。一旦某个形参提供默认实参，它右边所有形参都必须有默认实参。ht有默认值，但后面wd、bckgrnd没有默认，语法不允许。
规则：默认参数必须出现在形参列表靠右位置，有默认的参数右边不能出现没有默认的参数。
*/