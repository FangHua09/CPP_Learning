/*
题目描述：引用形参什么时候应该是常量引用？如果形参应该是常量引用，而我们将其设为了普通引用，会发生什么情况？

应当使用常量引用的场景1）函数仅读取参数，不会修改实参内容；2）对象体积较大（string、容器、自定义类），使用引用避免拷贝开销；3）希望函数支持传入字面值、表达式生成的临时对象。
本该 const 引用，却设置普通引用的后果1）普通非 const 引用不能绑定字符串字面值、临时对象，这类调用直接编译报错，函数可用场景被限制；2）失去 const 编译保护，函数内部可以意外修改外部实参，编译器不会告警。

核心知识点：
只读参数优先const &；需要修改实参才使用普通&。
*/