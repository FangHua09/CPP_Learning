/*
题目描述：下面的哪个调用是非法的？为什么？哪个调用虽然合法但显然与程序员的初衷不符？为什么？
char *init(int ht, int wd = 80, char bckgrnd = ' ');
(a) init();
(b) init(24,10);
(c) init(14,'*');

解答
(a) init(); ❌非法。第一个形参ht没有默认实参，调用时必须至少提供第一个实参，缺少 ht 实参编译报错。
(b) init(24,10); ✅合法。ht=24，wd=10；bckgrnd 使用默认空格。符合设计意图。
(c) init(14,'*'); ✅语法合法，但违背程序员初衷。实参按位置匹配：ht=14，wd='*'（char'*'隐式转换成 int ASCII 值 42）；bckgrnd取默认空格。本意用户想把'*'传给背景字符bckgrnd，但位置匹
*/