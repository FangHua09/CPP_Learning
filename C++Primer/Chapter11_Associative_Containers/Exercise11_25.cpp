/*
题目描述：对比下面程序与上一题程序
vector<int> v;
v[0] = 1;

知识点：vector下标运算符不会自动扩容；vector[]仅访问已有位置，下标越界属于未定义行为 UB；区分map[]与vector[]本质差异。
解答：
上一题map[0]=1：键不存在，自动插入元素，合法。
本题vector<int> v; v[0]=1：v为空，v[0]访问不存在的位置，越界，未定义行为，不会自动新增元素，程序可能崩溃 / 乱码。
核心区别：map[]不存在则插入；vector[]不做任何扩容检查。
*/
