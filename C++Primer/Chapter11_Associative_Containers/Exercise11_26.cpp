/*
题目描述：可以用什么类型来对一个 map 进行下标操作？下标运算符返回的类型是什么？请给出一个具体例子 —— 即，定义一个 map，然后写出一个可以用来对 map 进行下标操作的类型以及下标运算将会返回的类型。
知识点：map<K,V>下标运算符原型；下标参数类型为键类型K；返回类型是V&（mapped_type 的引用）；multimap不支持下标。
解答：
对于map<K, V>：
下标操作的参数类型：K（键类型，可以接收能隐式转为 K 的变量）
下标运算符返回类型：V&（值类型的引用）

示例代码：
map<string, int> myMap;
// map<K,V>：K = string，V = int
myMap["cpp"] = 100;


// 下标使用的类型：string
// myMap["cpp"] 返回类型：int&
*/
