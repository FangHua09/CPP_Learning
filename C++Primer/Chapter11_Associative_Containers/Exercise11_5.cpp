/*
题目：解释 map 和 set 的区别。你如何选择使用哪个？
区别
set：只保存键 (key)，每个 key 唯一，只关心元素是否存在，没有关联的值。底层红黑树。
map：保存键值对 pair<const key, value>，key 唯一，key 用来查找，value 是关联数据。底层红黑树。

选择
如果只需要判断元素是否存在，不需要附带数据 → 选set。
如果需要根据一个 key，查找对应的关联数据 → 选map。
*/
