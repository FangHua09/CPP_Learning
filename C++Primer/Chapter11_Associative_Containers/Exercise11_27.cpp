/*
题目描述：对于什么问题你会使用 count 来解决？什么时候你又会选择 find 呢？
知识点：关联容器 count、find 的作用区别；count 返回键的匹配个数；find 返回指向匹配元素的迭代器，找不到返回尾后迭代器。
解答：
使用count：只需要统计给定键有多少条记录，不需要拿到元素迭代器去读写内容。适合 multimap/multiset（允许重复 key）。
使用find：只要判断是否存在该 key，并且后续要访问 / 修改 / 删除这个元素。
对于 map/set（key 唯一）：count 只会返回 0 或 1，判断存在性用 find 效率更好，不用统计数量。

*/
