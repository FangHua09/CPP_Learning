/*
解释 set 和 list 的区别。你如何选择使用哪个？
区别
set：关联容器，红黑树，元素自动有序、不重复；查找 O (log n)；不能直接修改元素 key。
list：顺序容器，双向链表；元素可重复、顺序由插入顺序决定；查找需要遍历 O (n)；可在任意位置快速增删元素。

选择
需要快速查找元素、保证元素唯一，自动排序 → 选set。
频繁在中间插入删除，很少查找，允许重复元素 → 选list。
*/
