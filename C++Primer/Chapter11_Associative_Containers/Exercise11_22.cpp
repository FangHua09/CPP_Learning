/*
map<string, vector<int>>，写出insert的参数类型和返回类型。
参数类型：pair<string, vector<int>>
返回类型：pair<map<string, vector<int>>::iterator, bool>
说明：insert接收一个 map 的value_type（pair）；返回 pair：第一个元素是迭代器，第二个 bool 标记是否插入成功。
*/
