/*
编写程序实现文本查询，不要定义类来管理数据。你的程序应该接受一个文件，并与用户交互来查询单词。
使用 vector、map 和 set 容器来保存来自文件的数据并生成查询结果。
知识点
不用自定义类封装，直接在主函数内用容器保存全部数据；
vector<string>：存储文档每一行原文；
map<string, set<size_t>>：key 是单词，value 是该单词出现的行号集合，set 自动去重、升序；
istringstream 分割每行字符串提取单词；
循环交互：接收用户输入单词，查询并打印结果，输入 q 退出。
*/
