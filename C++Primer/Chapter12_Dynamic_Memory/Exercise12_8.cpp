/*
题目描述：下面的函数是否有错误？如果有，解释错误原因。
bool b() {
    int* p = new int;
    // ...
    return p;
}
    
知识点：指针隐式转换；内存泄漏。
解答
存在两处错误：
return p;：int*指针隐式转为bool。只要指针不是空指针，就会转为true。语法可以编译，但逻辑意图错误。
内存泄漏：new int分配的堆内存，没有执行delete p。函数返回后，局部指针p销毁，再也无法获取这块堆内存地址，内存丢失。
本质：分配了堆内存，没有释放，丢失内存地址。
*/

