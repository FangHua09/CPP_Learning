/*
假定 p1 和 p2 指向同一个数组中的元素，则 p1 += p2 - p1; 功能是什么？什么情况下非法？
功能p2‑p1：指针相减，得到两个指针之间的元素个数（距离）。p1 += (p2‑p1)：把这个距离加到p1上，最终让 p1 等于 p2，p1 和 p2 指向同一个元素。等价于 p1 = p2;。

非法情况
p1、p2不指向同一个数组：指针相减只允许同一数组内的指针；来自不同数组的指针做减法是未定义行为。
p2在p1前面，p2‑p1得到负数；指针依然可以加减负数，但前提必须是同一数组范围。核心限制：两个指针必须属于同一个数组，否则p2‑p1本身非法。
*/