/*
相比于 vector 来说，数组有哪些缺点，请列举一些。

数组大小固定，运行时不能改变大小；vector 可以动态增长，push_back方便添加元素。
数组维度必须是编译期常量表达式，不能用运行时变量指定大小。
数组不会自动初始化：局部内置数组元素是随机垃圾值；vector 可以统一初始化。
数组拷贝麻烦，不能直接赋值拷贝数组；vector支持直接拷贝、赋值。
使用数组容易越界；没有size()直接获取元素个数，需要自己计算；vector 有.size()。
数组名会隐式转为指针，丢失数组大小信息，函数传参时很容易出错。
没有丰富的成员函数；vector 有完整接口。
*/