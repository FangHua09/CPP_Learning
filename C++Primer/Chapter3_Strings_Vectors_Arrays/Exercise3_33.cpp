/*
104 页分数统计程序，如果不初始化 scores 会发生什么？
原题：vector<unsigned> scores(11,0);，11 个元素全部初始化为 0，用来统计各分数段计数。

如果不初始化：
若scores是局部 vector，内部元素会是随机垃圾值。
后续++*(scores.begin()+grade/10)是在随机值上做自增，统计结果完全错乱，得到错误计数。
计数容器必须初始全 0，否则原有垃圾值会干扰统计。
*/