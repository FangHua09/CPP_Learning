/*
题目：在equal例子中，如果两个名册保存的都是 C 风格字符串而不是string，会发生什么？
✅ 分析：
equal算法用==比较对应元素。
C 风格字符串是char*类型，char*的==比较的是指针存储的内存地址，不是字符串内容。
就算两个指针指向的字符串文字完全相同，只要不是同一个内存地址，equal就会返回false。
这就是==默认的操作


而std::string重载了==，会比较字符串实际内容。
重载了==
*/