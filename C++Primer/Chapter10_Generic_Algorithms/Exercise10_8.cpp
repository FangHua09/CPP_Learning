/*
问题：标准库算法不会改变它们所操作的容器的大小。为什么使用back_inserter不会使这一断言失效？
解答：
标准库算法本身不会直接修改容器大小，算法只对迭代器执行操作。
back_inserter是插入迭代器：当对这个迭代器赋值时，迭代器内部会调用容器的push_back()成员函数，由容器自己的成员函数完成元素添加，不是算法直接修改容器。
所以 “标准库算法不改变容器大小” 这个断言仍然成立✅。

*/