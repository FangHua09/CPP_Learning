/*
if (read(read(cin, data1), data2))
逗号表达式：逗号运算符，先执行左侧，再执行右侧；整个表达式的值为最右侧表达式的值。

执行顺序：
先执行内层：read(cin, data1)，从 cin 读入数据到data1，返回cin；
将上面返回的cin作为第一个参数，调用外层read(cin, data2)，读取数据到data2；
if 判断的条件就是外层read返回的cin流对象；流对象做条件判断：检测输入流是否有效（读取是否成功，没有出错、没有 EOF）。
一句话总结：连续读取 data1、data2；只有两次读取都成功，if 条件才成立。
*/