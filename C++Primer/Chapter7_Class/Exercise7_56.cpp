/*
什么是类的静态成员
用static修饰的类成员，不属于某个单独对象，归属于整个类；程序中只有这一份实例，所有对象共享。分为静态数据成员、静态成员函数。
优点
类所有对象共享一份数据，节省内存，不需要每个对象都存一份副本。
静态成员函数没有 this 指针，不依赖对象就可以调用类名::静态函数()。
静态成员可以保存类级别的全局状态（计数、利率、全局配置），封装在类作用域内，比全局变量更好，具备访问控制（private/public）。
和普通成员区别


普通成员 (非 static)	静态成员 (static)
归属	属于类的对象，每个对象独立副本	属于类本身，全类共享一份
this 指针	成员函数带有 this 指针	静态成员函数没有 this 指针
调用方式	必须通过对象调用 obj.member()	对象调用 / 类名::成员两种方式
存储位置	对象内存空间里面	不在对象内存，独立全局静态存储区
访问权限	普通成员函数可以访问静态、普通成员	静态成员函数只能访问静态成员，不能访问普通成员
*/