/*
问题：封装是什么？用处是什么？
封装含义：把数据成员与操作数据的函数组合到类中；将实现细节隐藏（放到 private），只对外暴露 public 接口。使用者只使用公开接口，不需要知道类内部数据与实现细节。
用处
防止外部代码随意修改内部数据，保证对象状态合法。
隔离实现与使用：类内部实现修改，只要 public 接口不变，外部调用代码不用改动。
降低代码复杂度，使用者只需要看对外接口，不用关心内部逻辑。
代码更容易维护、调试。
*/