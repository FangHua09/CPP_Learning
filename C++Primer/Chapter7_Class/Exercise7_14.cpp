/*
题目描述：编写构造函数，显式使用类内初始值初始化成员。
= default默认构造会使用类内初始值；本题要自己手写构造函数版本。

Sales_data()
{
    bookNo = "";
    units_sold = 0;
    revenue = 0.0;
}
对比：Sales_data() = default; 编译器生成版本，自动使用类内初始值；上面手写版本手动赋值同样效果。
注意：这是函数体内赋值，不是初始化列表。函数体内是赋值操作，不是初始化。
*/