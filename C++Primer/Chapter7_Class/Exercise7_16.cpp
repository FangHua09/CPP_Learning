/*
问题：类定义中访问说明符出现的位置、次数有限定吗？哪些成员放public，哪些放private？
访问说明符（public:、private:、protected:）没有次数限制，可以在类体中多次出现，位置也没有强制限制；每一个说明符改变其后成员的访问权限，直到下一个访问说明符或者类结束。只能写在类体内部，不能写在类体外。
public 之后：对外公开的接口，外部代码可以访问。一般放成员函数（类对外操作接口）。
private 之后：类的内部实现细节，外部不能访问。一般放数据成员，隐藏对象状态。
设计原则：数据私有，接口公开。
*/