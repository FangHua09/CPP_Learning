/*
练习 5.1：什么是空语句？什么时候会用到空语句？
空语句：就是只有一个分号 ; 的语句。
; // 空语句
空语句不做任何实际操作，语法上需要一条语句，但逻辑上不需要执行任何动作时使用。
使用场景举例：

循环体什么都不用做，把全部逻辑放在条件表达式里。
// 读取输入直到遇到换行，循环体为空
while (cin.get() != '\n') 
    ;
为了语法完整，防止遗忘分号；注意不要误写多余分号造成 bug。
⚠️坑：if(cond); do_something(); 这里 if 后面跟了空语句，do_something()无论条件真假都会执行。
*/