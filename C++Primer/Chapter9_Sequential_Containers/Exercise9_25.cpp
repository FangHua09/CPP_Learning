/*
对于第 312 页中删除一个范围内的元素的程序，如果 elem1 与 elem2 相等会发生什么？如果 elem2 是尾后迭代器，或者 elem1 和 elem2 皆为尾后迭代器，又会发生什么？
第 312 页程序核心函数调用：c.erase(elem1, elem2);，删除迭代器区间 [elem1, elem2)（左闭右开）

elem1 与 elem2 相等区间[elem1, elem1)是空区间，erase()不会删除任何元素，容器内容保持不变，返回值为elem1，操作合法。

elem2 是尾后迭代器（c.end()）删除从elem1开始，直到容器末尾的所有元素，操作合法。

elem1 和 elem2 都为尾后迭代器（都等于c.end()）区间[end(), end())是空区间，不删除任何元素，容器不变，操作合法。

前提条件：elem1、elem2 必须是同一个容器的迭代器，且elem1到elem2是有效正向区间。
*/