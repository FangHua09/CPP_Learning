/*
题目描述：假定你希望每次读取一个字符存入一个string中，而且知道最少需要读取 100 个字符，应该如何提高程序的性能？

分析：
string内部是连续内存，反复push_back()单个字符时，如果元素数量到达内存上限，会触发内存重分配（开辟新内存、拷贝旧字符、释放旧内存），开销比较大。
优化方案：调用string::reserve(100)，预分配至少可以存放 100 个字符的内存。
这样读取前 100 个字符时不会触发内存重分配；如果后续字符超过 100 个，string 依旧会自动扩容。
*/