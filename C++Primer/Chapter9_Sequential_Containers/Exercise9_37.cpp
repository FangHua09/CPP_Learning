/*
题目描述：为什么 list 或 array 没有 capacity 成员函数？
解答
list是双向链表，元素分散在堆上，不需要连续内存，不存在 “预分配一块连续内存、预留多余空间” 的机制，没有 capacity 概念。每新增元素单独分配节点。
array是固定大小数组，编译时大小确定，size 永远等于它的数组大小，内存不会动态增长，没有预留空间，因此不需要 capacity。
capacity 只针对 vector、string 这种连续内存、会预分配预留空间的容器。
*/