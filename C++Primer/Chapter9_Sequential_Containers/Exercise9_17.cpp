/*
题目：假定 c1 和 c2 是两个容器，if (c1 < c2)，下面的比较操作有何限制（如果有的话）？

限制条件：
c1、c2 必须是相同类型的容器，并且元素类型也要完全相同。不能拿list<int>和vector<int>做<比较。
容器的<是字典序比较，该比较依赖于元素类型支持<运算符；容器内存储的元素必须定义了<。
原理：容器的关系运算符（< > <= >= == !=）只适用于同类型容器。比较逻辑：按字典序逐元素对比，找到第一处不相等元素，用该元素的<结果作为容器比较结果；如果前面全部相等，短的容器认为更小。
*/