/*
## 练习 1.6：判断程序片段合法性

原代码片段：
std::cout << "The sum of " << v1;
    << " and " << v2;
    << " is " << v1 + v2 << std::endl;


1. **不合法，编译报错**原因：第一行末尾有分号`;`，**结束了这条完整语句**。第二行开头的 `<<` 没有左操作数（前面没有`std::cout`），`<<`运算符找不到左侧流对象，语法错误。
2. 修正方案方案 1：去掉多余分号，保持链式调用
*/