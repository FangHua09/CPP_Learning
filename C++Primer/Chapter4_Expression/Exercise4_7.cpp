/*
溢出含义
算术溢出：计算结果超出该类型能够保存的数值范围。有符号整数溢出属于未定义行为；无符号整数溢出是定义好的（模 2ⁿ回绕）。简单说：运算得到的值太大 / 太小，存不下，发生溢出。
三条溢出表达式示例（int 为 32 位常见环境）
cpp
运行
int max_int = 2147483647;
max_int + 1;          //1. int最大值+1，有符号溢出

int min_int = -2147483648;
min_int - 1;          //2. int最小值‑1向下溢出

2147483640 * 10;      //3. 两数相乘结果超出int范围溢出
补充：unsigned 溢出是定义行为；signed int 溢出标准规定为未定义行为，不同编译器表现可能不一样。
*/