/*
教材原版：quiz1原始类型是 unsigned long，用来保存 30 位学生测验标记，C++ 标准规定unsigned long至少 32 比特，足够容纳 30 个学生的状态位。
题目：如果换成 unsigned int 会发生什么？
C++ 标准只规定：
unsigned long：最少 32 位，一定存得下 30 位学生标记。
unsigned int：最少只要求 16 位，不同机器长度不一样：有的机器 32 位，有的老式机器只有 16 位。
两种情况：
如果平台上unsigned int是 32 位：程序可以正常运行，位运算行为正常。
如果平台上unsigned int只有 16 位：我们要操作第 16~29 位，但是变量只有 16 比特，移位访问高位会越界，行为未定义，无法保存全部 30 名学生的测验信息，高位数据丢失。
核心考点：不是右移补 0 的区别；重点是类型最小尺寸标准。unsigned int不能保证 >=32 位，可移植性差；unsigned long标准强制至少 32 位，适合这个存 30 个标志位的场景。
*/