/*
原题循环（后置版本），遍历 vector 输出元素：
vector<int>::iterator it = vec.begin();
while (it != vec.end()){
    cout << *it << endl;
    it++;
}


如果改成前置递增 ++it。
✅ 结果完全一样。
这里递增表达式单独作为一条语句，没有使用递增的返回值，只是把迭代器向后移动。it++ 和 ++it 副作用都是迭代器自增；丢弃返回值时，循环行为、输出结果不变。


补充：只有当把++it / it++的返回值参与别的运算时，才会出现差异。
*/