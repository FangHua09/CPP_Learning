/*
if (42 = i)   // ...
if (i = 42)   // ...


if (42 = i)：编译报错。赋值运算符左侧必须是可修改左值，字面量42是常量，不能作为赋值左操作数，编译器直接拒绝。
if (i = 42)：合法编译通过，这是赋值，不是相等判断。把 42 赋值给i；赋值表达式结果为42，非 0，条件永远为真。这是典型笔误，本意大概率是if(i == 42)。
*/