/*
(a) if (p = getPtr() != 0)
问题原因：运算符优先级!= 的优先级高于赋值运算符=。原表达式等价于：
if ( p = (getPtr() != 0) )
实际行为：先调用getPtr()，判断返回指针是否不等于 0，得到布尔值true(1)或false(0)；再把这个 0/1 赋值给指针变量p。
预期意图：把getPtr()返回的指针赋值给p，然后判断指针p不为空。
结果：代码语法合法，但逻辑完全错误，把布尔值赋给指针，不是把指针赋给p。
✅ 修改方案，增加括号改变运算顺序：
if ((p = getPtr()) != nullptr)


(b) if (i = 1024)
问题原因：把赋值运算符=误写为相等比较==
实际行为：i = 1024是赋值表达式，将1024赋值给变量i；表达式结果为1024，非 0，所以 if 条件永远为真。
预期意图：判断变量i的值是否等于 1024。
✅ 修改方案：使用相等运算符==
if (i == 1024)
*/