/*
 *
 练习 14.1：重载运算符和内置运算符的异同
 ✅ 相同之处
 优先级、结合性不变：重载运算符不会改变运算符原来的优先级、结合规则。
 操作数个数不变：一元运算符重载后依旧一元；二元依旧二元。
 ✅ 不同之处
 内置运算符不能被重载：::、.、.*、?: 不能重载。
 重载运算符不能创建新运算符。
 内置运算符的类型规则不适用重载版本：内置运算符会做隐式类型转换；重载运算符本质是函数调用，参数转换遵循函数参数转换规则。
 重载运算符的求值顺序不一定保留：内置 && / || 有短路求值；重载的 &&、|| 是普通函数，没有短路求值。
 内置运算符可以操作内置类型；重载运算符至少要有一个操作数是类 / 枚举类型。
 一句话总结：优先级、结合性、操作数数量不变；短路求值、内置类型转换规则会不一样。
 */

