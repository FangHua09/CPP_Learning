/*
题目：下面的哪个定义是不合法的？为什么？
(a) int ival = 1.01;
(b) int &rval1 = 1.01;
(c) int &rval2 = ival;
(d) int &rval3;

引用规则：引用必须绑定到已经存在的对象（左值），引用定义时必须初始化，不能绑定字面值常量。
(a) int ival = 1.01; 合法double 赋值给 int，小数截断，ival得到1，编译器警告精度丢失，语法允许。
(b) int &rval1 = 1.01; 非法普通 int 引用不能绑定浮点字面值常量，引用必须绑定变量对象，不能绑定临时常量。
(c) int &rval2 = ival; 合法引用绑定 int 变量ival。
(d) int &rval3; 非法引用定义时必须初始化，没有初始化的引用不允许。
非法：(b)、(d)。
*/