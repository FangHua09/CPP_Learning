/*
**题目**：类型 int、long、long long 和 short 的区别是什么？无符号类型和带符号类型的区别是什么？float 和 double 的区别是什么？

**答案**

1. `short`、`int`、`long`、`long long`：都是**带符号整数类型**，区别在于**最小存储空间大小不同**（C++ 标准规定最小尺寸）
   - `short`：至少 16 位
   - `int`：至少 16 位，一般机器上 32 位
   - `long`：至少 32 位
   - `long long`：至少 64 位（C++11 新增）> 
   > 实际占用字节由编译器 / 系统决定，存储空间越大，可以表示的数值范围越大。

2. **无符号 (unsigned) 与带符号 (signed)**
   - 带符号类型：可以保存**正数、负数、0**；最高位作为符号位。
   - 无符号类型：只能保存**大于等于 0 的非负数**，全部二进制位都用来存数值；无符号数永远不会出现负数，如果赋值负数会发生绕回（模运算）。
   - 默认：`short`/`int`/`long`/`long long`默认是**带符号**；加`unsigned`变成无符号。
   
3. `float` 和 `double`（浮点类型）
   - `float`：单精度，一般 32 位；大约 6‑7 位有效数字。
   - `double`：双精度，一般 64 位；大约 15‑16 位有效数字。
   - double 精度更高、可表示范围更大；大多数计算优先用`double`，float 精度容易不足。
*/