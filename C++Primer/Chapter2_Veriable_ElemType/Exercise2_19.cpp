/*
说明指针和引用的主要区别

指针是变量，占用内存空间，存放地址；引用只是对象别名，不占用独立内存。
指针可以修改指向，可以先后指向不同对象；引用初始化绑定对象后，永远不能更改绑定，只能修改绑定对象的值。
指针可以不初始化，可以为空指针nullptr；引用定义时必须初始化，不能为空。
指针使用*解引用、&取地址；引用直接使用，语法和普通变量一致。
指针有多级（int **）；不存在多级引用。
*/